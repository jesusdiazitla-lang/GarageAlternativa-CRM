# ============================================
# Variables de Entorno - Backend
# ============================================
# 
# INSTRUCCIONES:
# 1. Copiar este archivo como .env
# 2. Reemplazar los valores de ejemplo con tus propios valores
# 3. NO subir el archivo .env a Git (ya está en .gitignore)
#

# ===== SERVIDOR =====
PORT=3000
NODE_ENV=development

# ===== BASE DE DATOS =====
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=garage_alternativo

# ===== JWT (JSON Web Tokens) =====
# Genera una clave secreta fuerte y única
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_secreta_super_segura_cambiame_por_algo_aleatorio
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# ===== CORS =====
# URL del frontend para permitir peticiones
FRONTEND_URL=http://localhost:5173

# ===== SCHEDULER =====
# Hora de ejecución del cron de alertas (formato: minuto hora * * *)
# Por defecto: 6:00 AM todos los días
ALERT_CRON_SCHEDULE=0 6 * * *

# ===== LOGS =====
LOG_LEVEL=info

# ===== NOTIFICACIONES (Futuro) =====
# Descomenta cuando implementes WhatsApp/Email
# WHATSAPP_API_KEY=
# WHATSAPP_PHONE_NUMBER=
# SENDGRID_API_KEY=
# EMAIL_FROM=noreply@garagealternativo.com